export interface Member {
    memberId: number;
    memberLastName: string;
    firstName: string;
    email: string;
    password: string;
    address: string;
    city: string;    
    postalCode: string;
    phone: string;
    access: number;
}